<% provide(:title, "#{@user.name}") %>
<p id="notice"><%= notice %></p>

<p>
    <% if @user.avatar.attached? %>
    <%= cl_image_tag @user.avatar.key, :width=>50, :crop=>"fill" %>
    <% end %>
</p>
<p>
    <%="Hello, #{current_user.name.titlecase}"%>
</p>
<p>
    <%
=begin%>
 <%=link_to 'Groups', groups_path%> 
<%
=end%>
    <%= link_to 'Edit', edit_user_path(@user) %>
    <%=link_to 'Log out', logout_path%>
</p>
    <p class="text-center">
      <strong class="text-grey text-center">Agile Tasks & Activities</strong>
    </p>